resources:
  pipelines:
    process_customer_reviews:
      name: Process Customer Reviews
      libraries:
        - glob:
            include: ../src/process_customer_reviews/transformations/**
      catalog: ${var.catalog}
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src/process_customer_reviews
      schema: ${var.schema}
      serverless: true
